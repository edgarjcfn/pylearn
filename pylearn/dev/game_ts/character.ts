module Pylearn {
    export class Character extends Phaser.Sprite {
    	constructor(game:Phaser.Game, x: number, y: number) {
    		super(game, x, y, 'pirate', 0);
    	}

    	create() {
    	}
    }
}